import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  safeArea: {
    flex: 1,
    backgroundColor: 'black',
  },
  operationText: {
    color: 'white',
    fontSize: 40,
    textAlign: 'right',
  },
});
